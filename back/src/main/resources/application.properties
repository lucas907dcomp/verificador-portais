spring.application.name=back
# --- H2 Database Configuration ---

# Habilita o console web do H2 para voc� visualizar o banco no navegador
spring.h2.console.enabled=true

# URL de conex�o. "mem" significa que � um banco em mem�ria. "testdb" � o nome do banco.
spring.datasource.url=jdbc:h2:mem:testdb

# Driver do H2 (geralmente o Spring infere isso, mas � bom ser expl�cito)
spring.datasource.driverClassName=org.h2.Driver

# Usu�rio e senha padr�o do H2
spring.datasource.username=sa
spring.datasource.password=

# --- JPA Configuration ---

# Permite que o Spring Data JPA crie e atualize as tabelas automaticamente com base nas suas classes @Entity
spring.jpa.hibernate.ddl-auto=update

# Mostra no console os comandos SQL que o Hibernate est� executando (�timo para debug)
spring.jpa.show-sql=true

# Configurações do Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true

# Configurações do servidor
server.port=8080

# Configurações do Banco de Dados
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configurações do Banco de Dados H2
spring.h2.console.path=/h2-console